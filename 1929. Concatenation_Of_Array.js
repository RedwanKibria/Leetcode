/**
 * @param {number[]} nums
 * @return {number[]}
 */
var getConcatenation = function(nums) {
  var firstArray = nums;
  var secondArray = nums;
  var result = firstArray.concat(secondArray);

  return result;  
};
